# Coursework Notes

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## Notes

This repository keeps my practice scripts for machine learning, deep learning, and econometrics.

The examples use synthetic or built-in datasets so I can rerun them later without private class data. For course submission, I can replace the data-generation parts with the assigned datasets and keep the same model structure.

Current folders:

- `Machine Learning`
- `Deep Learning`
- `计量经济学`
- `docs`
